/**
 * Feature-local types (UI state, filters, local only).
 *
 * For API types, import from @/api/{domain}/types instead.
 *
 * Example:
 *   import type { Document } from "@/api/documents/types";
 *
 *   export interface MyFeatureFilter {
 *     documentId: string;
 *     sortBy: "date" | "name";
 *   }
 */
