/**
 * Feature API: public exports.
 *
 * Keep this minimal — only what other features/pages need.
 * Internal re-exports (sub-components, helpers) stay in their own files.
 */

export { TemplateView } from "./components/TemplateView";
export { useTemplateExample } from "./hooks/useTemplateExample";
